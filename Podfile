# Uncomment the next line to define a global platform for your project
platform :ios, '10.10'

target 'WishList' do
  use_frameworks!

  # Pods for WishList
  pod 'OHMySQL', '~> 2.1'
  pod 'SwiftWebSocket'
  pod 'Socket.swift', '~> 2.2'

end
